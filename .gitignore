# === UE5 Generated ===
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
*.sln
*.xcodeproj
*.xcworkspace

# === VS ===
.vs/
*.user
*.filters
*.opendb
*.opensdf
*.sdf
*.suo
*.ipch
*.sln.docstates

# === OS ===
Thumbs.db
.DS_Store
Desktop.ini

# === Web（WebReplay，D-21）===
WebReplay/node_modules/
WebReplay/dist/
WebReplay/.vite/
WebReplay/*.local

# === 本机开发工具配置（不入库）===
# MCP 服务器配置：给 AI 助手查 UE 资产用的工具集，属于个人开发环境而非项目产物
.mcp.json

# === Backend（DirectorService · Java 17 + Spring Boot + Maven）===
# 用 Maven 不用 Gradle：本机已装 Maven 3.9.16、未装 Gradle，Spring Boot 两者支持相同，
# 没有理由为此再装一套构建工具。故忽略的是 target/ 而非 build/ 与 .gradle/。
DirectorService/target/
DirectorService/*.log

# ★ 本地配置里有 LLM 的 API key，绝不能入库。
# 本次开工的全部意义是「key 不进客户端」，key 若进了 git，
# 就是把一个安全问题换成了一个更严重的安全问题（客户端的 key 只泄露给玩家，
# 仓库里的 key 泄露给全世界，且 git 历史删不干净——见 2026-07-29 那次 filter-repo）。
# CI 与生产走环境变量注入，本地走这个文件。
DirectorService/src/main/resources/application-local.yml
DirectorService/src/main/resources/application-local.yaml

# === Misc ===
*.log
*.crash
*.trace

# === 引擎自带示例内容/占位模型（体积大，非团队自制资源，未配置 Git LFS 故不入库） ===
# 需要时在编辑器里 Content Browser -> Add -> Feature or Content Pack 重新添加 "Top Down" 模板即可拿回
UnrealProject/Content/测试用俯视角开发新手包/
# Manny/Quinn 占位骨骼网格体，随引擎/模板内容包自带，同样可通过上面方式重新获取
UnrealProject/Content/Characters/Mannequins/
UnrealProject/Content/Characters/Mannequin_UE4/
# TopDown 示例地图（World Partition）自己的外部 Actor/Object 伴生数据，同属示例包不入库
# 注意：我们自己的关卡（如 L_TestArena）不是 World Partition 关卡，不会产生这类文件
UnrealProject/Content/__ExternalActors__/TopDown/
UnrealProject/Content/__ExternalActors__/测试用俯视角开发新手包/
UnrealProject/Content/__ExternalObjects__/TopDown/
UnrealProject/Content/__ExternalObjects__/测试用俯视角开发新手包/
