# Changelog

本项目的显著变更记录。范围与取舍的唯一权威是 [`Docs/DECISIONS.md`](Docs/DECISIONS.md)，
本文件只记「发生了什么、结论是什么」。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [未发布] — 第八次开工：数据回流与参数校准（D-24 / D-25）

### 新增

- **决策流水落库**（`DirectorService/`）：每次决策请求的**原始输入拆成列**存入 MySQL。
  存指纹只能回答「这个方案命中率多少」，存原始字段才能回答「换个方案会怎样」——
  **两者成本一样，漏存了永远补不回来**。另单独存一份当时的指纹，作为模拟器的正确性锚点。
- **异步落库**：有界队列（1000）+ 单消费线程 + 批量刷（50 条 / 1 秒），四条 Micrometer 指标。
- **聚合接口**：`/v1/analytics/summary`、`/cache/simulate`、`/fingerprint/split-contribution`。
- **指纹方案模拟器**：在历史流水上重放不同方案，回答「换一套会怎样」。
- **调试作弊开关**（D-25）：`shm.Debug.PlayerDamageMult` / `PlayerMaxHPMult` /
  `RangedDamageMult`，**默认 1.0 即不生效**；开启时请求带 `X-SHM-Debug` 头，
  服务端存入 `debug_flags` 列，**使作弊样本与真实样本永久可分**。

### 修复

- **分桶函数的边界缺陷**（踩坑 #34）：`(int)(v / width)` 让**满分 100 永远自成一桶**
  （桶宽 20 下 `96.8 → 桶4` 而 `100 → 桶5`），恰恰违背分桶「87 和 85 不该算两种玩家」的初衷。
  而样本中 `buildConcentration == 100` 占 **81%**，**这个缺陷几乎每次都命中**。
  修法 `min(raw, (100-1)/width)`；同批数据指纹 **15 → 12**、模拟命中率 **28.1% → 46.9%**、
  桶宽单调性恢复。
  > ⚠️ 这是本项目**第一次改动线上指纹的行为**。允许的理由是**它是缺陷修复不是参数调优**——
  > 分桶函数没有实现它声称的语义。判据：**换一批数据这个结论会不会变**。
- **三处「旁路渗进主链路」**（#31）：测试数据污染开发库 · `isAvailable` 是启动快照 ·
  jdbc starter 自动注册健康指示器导致数据库一挂 `/actuator/health` 就 503。
- **`spring.sql.init` 让建表成了启动硬依赖**（#30）：改为 `ApplicationReadyEvent` 建表并整段兜住。
- **用「睡一会儿」等异步导致的 flaky 测试**（#33）：改为等真实条件，连跑三次确认。

### ⭐ M5-5 校准结论（这是整个 M5 的目的）

> **两个参数（桶宽 20 分、候选 3 条）当初是拍的，代码注释里写明了「待回流数据校准」。
> 校准做完了 —— 而结论是：这两个数字一个都没改，因为数据说它们不是主要矛盾。**

**① 原假设被推翻。** 原以为是 `floorIndex` 把一局的两次请求切成两条指纹。
数据显示 `floor` / `budget` / `confidence` 三者**一一对应**，
但**三个一起去掉，指纹数纹丝不动**。
**真正的切碎源是 `decisionHistory`**：F1 历史恒空、F2 恒非空，本身就是层号的代理；
更糟的是它的**内容**取决于上一层 LLM 选了什么规则，**等于给每条指纹加了随机后缀**。

**② 放宽桶宽确有效果，但证据不能用。** 第二批 43 条（**重放保真度自检首次通过：
模拟命中率 16.3% == 实测 16.3%，逐条对上，算法漂移 0**）：

| 桶宽 | 指纹数 | 命中率 |
|---|---|---|
| **20（当前）** | 26 | 16.3% |
| 34 | 18 | 25.6% |
| 50 | 13 | 37.2% |
| 100 | 9 | 53.5% |

> ⚠️ **但这 43 条全部是调试作弊状态下采的**（`debug_flags` 非空）。
> 作弊抬高了战斗效率的速度分，所以这批数据能回答**结构问题**，
> **不能**回答**比率问题**。**要改这个数字，需要一批真实游玩的数据。**
> 拿作弊数据去调一个比率参数，就是「用一个可控的替代问题替换真实问题」。

**③ 候选数的代价第一次被量化**：候选 1 → 39.5%、2 → 20.9%、**3 → 16.3%**、5 → 14.0%。
候选数从 3 降到 1 命中率是 2.4 倍，**但候选数存在的全部理由就是「同类玩家别听到同一句台词」**——
**这是体验与成本的取舍，不是一个可优化的参数。**

**④ 画像五维里有两维信息量不足**（踩坑 #36）：`resourceSurplus` 恒为 50（D-09 无数据源，
刻意不入指纹）；`survivalPressure` 实际只有 `{0, 25}` 两个取值 ——
它测的是「有没有濒死过」而不是「挨了多少打」，且 LowHp 是闩锁、全项目无回血来源。
**真正在区分玩家的只有 3 维 + 1 bit** —— 这直接解释了为什么切碎指纹的是 `decisionHistory`。

### 变更

- 测试：服务端 **47 → 84**（0 跳过）；三端合计 **231** 全绿。
- `Docs/TestResults.md` 改为**由 `AutoTest.log` 生成**（`Docs/gen_test_results.py`），不再手写数字。
- 决策记录 **25 条**（D-01 ~ D-25）· 踩坑记录 **36 条**。

---

## [v1.1.0] — 第七次开工：决策网关后端（D-23）

### 新增

- `DirectorService/`（Java 17 + Spring Boot 3.2.5）：持有 API key、拼 prompt、调 LLM、缓存。
  **存在理由只有两条**：① key 不能进客户端 ② 决策数据要能跨局聚合。
- 第四个 Provider `FSHMRemoteProvider`（客户端侧），**决策编排代码一行没改**。
- Resilience4j 熔断（COUNT_BASED）· Bucket4j 双维度限流 · 语义指纹分桶缓存 ·
  Micrometer 指标端点。

### 关键取舍

- ⭐ **否决「把四道护栏搬上服务端」**：① 破坏「断网可玩」不变量 ② C++/Java 双写必然漂移
  ③ 单机游戏客户端本来就是权威，**为一个不成立的信任边界付双写代价是纯亏**。
- **刻意不加重试**：服务端上游 10s < 客户端 12s，**重试的结果没人接收**。
- **响应体不加信封**（meta 走 `X-SHM-*` 头）→ body 与 LLM 原始输出、回放脚本字节级同格式。

### 修复

- ⭐ **缓存在真实流量下形同虚设**：11 条单测全绿、同一请求连发 12 次完美命中，
  **而真实打 3 局一次都没命中**。根因是「未攒满 3 条候选不命中」，
  而一局只发 2 次请求、F1/F2 天然是两条指纹 —— 攒满要连打 4 局以上。
  改为**边用边攒**。教训写在 `CacheUnderRealTrafficTest` 里：
  **验证缓存必须用真实的访问模式。**

---

## [v1.0.0] — 第六次开工：Web 决策回放器（D-21）

- `WebReplay/`（Vue 3 + TS + Vite），**护栏前后对照**、画像雷达图、拖拽载入。
- [Live Demo](https://roseri66.github.io/ShanHaiMirror/) 长期在线。
- ⭐ **修掉演示数据的造假嫌疑**：样例里第 4 层在真实对局（`TotalFloors=3`）到不了。
  **是人工审阅发现的，机器测试一条都没抓到。**
  修法：补真实样例 + `_kind` 标注 + 页面出处横幅。

---

## [sprint-1-done] — 第一 ~ 第五次开工：MVP

决策链路 ①–⑦ 全部落地，**断网 / 无 key 完整可玩**，且可见（导演报告卡）、
可对照（`SHM.Director 0/1`）、可回放（镜界时间轴 + 决策日志）。

四条不变量确立：
① 数值只在链路第 ⑥ 步产生（**由类型系统强制**）· ② LLM 这一步可以整体失败 ·
③ 画像分析是纯函数 · ④ 不信任 LLM 输出是设计前提而非补丁。
